"""Quality report generation."""

from __future__ import annotations


def generate_report(metrics, output=None):
    """Render quality assessment results into a human-readable report.

    Planned outputs (later stage): Markdown / HTML report with per-channel
    metric tables and flagged anomalies.
    """
    raise NotImplementedError("Report generation arrives in a later stage")
